export enum Resolution {
    width = 1920,
    height = 1080
}

export enum MessageActions {
    Upscale = 'upscale',
    Downscale = 'downscale',
    SetPlayerMovement = 'set-player-movement',
    SetPlayerDirection = 'set-player-direction',
    MovePlayerBy = 'move-player-by',
    ShowInventory = 'show-inventory',
    HideInventory = "hide-inventory"
}

export enum Scale {
    Up = 'up',
    Down = 'down'
}

export enum Character {
    Player = 'player',
    Charsi = 'charsi',
    Akara = 'akara',
    Gheed = 'gheed'
}